# 项目硬化报告（测试 / 安全 / 性能 / 健壮性）

> 分支：`feature/hardening`（基于 `main`）。所有改动均在该分支，`main` 未受影响，便于你审阅与回滚。
> 完成时间：2026-06-14。执行人：Claude（全程主导）。

本次按既定四点推进：**①补关键路径测试 → ②安全审计并修复 → ③前端拆包懒加载 → ④v-if 链健壮性梳理**，本文为验收说明。

---

## 总览

| 点 | 结论 | 关键产出 |
|----|------|----------|
| ① 测试 | ✅ 完成 | 4 个测试类、**18 个用例全绿** |
| ② 安全 | ✅ 关键项已修复，其余已列明 | 修复简历越权/鉴权失效、上传后缀绕过；社区审核鉴权等列入待办 |
| ③ 拆包 | ✅ 完成 | 首包 gzip **835KB → ~378KB（约 -55%）**，echarts 378KB 不再进首屏 |
| ④ v-if | ✅ 梳理完成 | 唯一脆弱点（已修）确认无回归；其余分发链全部核对干净 |

提交记录（本分支）：
- `d58eeb3` test(backend)：关键路径单测 18 用例
- `8cb94d9` fix(security)：越权/鉴权/上传加固
- `24141c5` perf(frontend)：路由懒加载 + 拆包
- 基线 `229dc10` / `7c46528`（图标重复渲染修复 + 文字垂直对齐，已在 main）

---

## ① 后端关键路径测试

新增 `backend/src/test/java/com/resume/`，采用**服务/仓库层单元测试**（构造隔离的内存仓库 + Mockito 桩，不依赖 SQLite，快速确定）。

| 测试类 | 覆盖 | 用例数 |
|--------|------|--------|
| `AuthServiceImplTest` | 注册、重复名拒绝、BCrypt 校验、登录成功/失败、登出失效、改密 | 6 |
| `QuotaServiceImplTest` | AI/导出每日额度计数、耗尽拦截、兑换余额兜底、不限制 | 4 |
| `ResumeOwnershipTest` | 简历归属隔离：越权读/改/复制/删除全部被拒 | 4 |
| `RedeemCodeTest` | 会员码开通、额度码累加余额、重复使用拒绝、未知码报错 | 4 |

运行：`cd backend && mvn test` → `Tests run: 18, Failures: 0, Errors: 0`。

> 说明：这些测试同时充当 ②安全修复的回归保障（归属隔离、额度扣减逻辑未因改动而退化）。

---

## ② 安全审计与修复

### 修复清单（本次已改）

| 级别 | 问题 | 位置 | 修复 |
|------|------|------|------|
| 🔴 严重 | **简历接口鉴权形同虚设**：拦截器写 `/resume/**`，控制器实际是 `/resumes`，模式不匹配→所有简历接口免登录可访问 | `WebMvcConfig` | 路径改 `/resumes/**`，并排除公开分享 `/resumes/share/*` |
| 🔴 严重 | **水平越权（IDOR）**：`userId` 由 `@RequestParam`/请求体传入，传 `?userId=N` 即可读写他人简历 | `ResumeController` 等 | 新增 `@CurrentUserId`，userId 统一取自 **Session**，客户端无法伪造 |
| 🟠 高 | `versions`/`restore`/`share`/`getShare` **完全无归属校验**，凭 resumeId 即可操作任意简历 | `ResumeController` | 增加 `notOwned()` 归属校验 |
| 🟠 高 | 面试记录、会员兑换/流水 **免登录 + IDOR** | `InterviewController`、`MemberController` | 纳入拦截 + `@CurrentUserId`；提交以登录态覆盖请求体 userId |
| 🟡 中 | **上传后缀取自用户文件名**：`image/png` 内容配 `evil.html` 后缀→落地为可被当 HTML 解析的文件（存储型 XSS） | `FileUploadServiceImpl` | 扩展名改由**已校验的 contentType** 推导；上传纳入鉴权 |

新增机制 `@CurrentUserId`（`config/CurrentUserId.java` + `CurrentUserIdArgumentResolver.java`）：从服务端 Session 注入登录用户 ID，是消除这一类越权的统一手段。

### 审计为「低风险/无问题」

- **SQL 注入**：未发现。业务走内存仓库；`PersistenceStore` 用参数化 `?` 占位（表/列名为代码常量，非用户输入）；MyBatis 无 `${}` 拼接。

### ⚠️ 待办（本次**未改**，建议尽快跟进）

| 级别 | 问题 | 建议 |
|------|------|------|
| 🔴 严重 | **社区审核/删除接口无鉴权**：`PUT /community/cases/{id}/approve`、`DELETE /community/cases|articles/{id}`、`GET /community/admin/*` 在 `/community/**` 下，未被 `AdminAuthInterceptor`（只管 `/admin/**`）覆盖→任何人可审核/删除任意内容 | 将这些迁到 `/admin/community/**` 并纳入管理员拦截，或加方法级管理员校验 |
| 🟠 高 | 社区投稿/点赞、模板收藏仍从客户端取 `userId`（`CommunityController`、`TemplateController`）：可冒名投稿/点赞、读他人点赞 | 同样改用 `@CurrentUserId`（机械替换即可，我已在其它控制器示范） |
| 🟡 中 | 仓库 `ownedBy` 把 `userId == null` 视为演示用户 1（兜底默认） | 受保护接口已由登录态兜底；建议长期去掉「null→1」默认，未登录直接拒绝 |
| 🟡 中 | Session Cookie 未显式设置 `HttpOnly`/`Secure`/`SameSite`，无会话固定防护 | 配置 cookie 属性；登录成功后 `changeSessionId()` 防固定 |

> 我把社区审核鉴权与社区/模板 IDOR 列为「待办」而非本次强改，是因为它们牵涉公开浏览路径与前端调用约定，需你确认产品取舍后再动，避免误伤公开访问。修复范式与已改控制器完全一致。

### 行为变化（需注意）

简历、面试等接口现在**必须登录**（修复前因路径 bug 实际不需要）。前端是 Session 登录、且会带 Cookie，登录后正常；**未登录的「演示模式」直接访问这些接口会返回 401** —— 这是正确的安全姿态，但若你依赖免登录演示，请确认。

---

## ③ 前端代码拆分与懒加载

**改动**：
- `src/router/index.js`：视图改为 `() => import()` 路由级懒加载（每页独立 chunk）。
- `vite.config.js`：`manualChunks` 拆出 `echarts` / `element-plus` / `vue-core` / `vendor`。

**效果（`vite build` 实测）**：

| | 改造前 | 改造后（着陆/首页首屏） |
|---|---|---|
| 入口 JS | 单包 **2502 KB / gzip 835 KB**（含 echarts，全部首屏加载） | index+vue-core+vendor+element-plus+Home ≈ **gzip ~378 KB** |
| echarts（1125 KB / gzip 378 KB） | 进首屏 | **不进首屏**，仅图表页（编辑器/后台/个人）按需加载（`index.html` 引用 echarts 次数 = 0） |
| 其余页面 | 全在首包 | 各自独立 chunk，按路由加载 |

首屏 JS 体积约 **-55%**，最大的 echarts 完全延后。

**可进一步优化（未做，已记录）**：`main.js` 是 Element Plus 全量引入，无法摇树。引入 `unplugin-vue-components` 按需导入可再砍掉一大块 element-plus 体积（改动较大，留作后续）。

---

## ④ 编辑器 v-if 链健壮性梳理

**起因**：本次最初修复的「图标文字重复渲染」就是一条类型分发链被中途插入的 `v-if`（字数统计 span）截断，导致兜底 `v-else` 错配、内容渲染两遍。

**梳理范围与结论**：

| 组件 | 链类型 | 结论 |
|------|--------|------|
| `DragResumeCanvas` | 组件类型分发（13 分支 + 编辑态 + 兜底） | 唯一出问题处，**已修**：字数统计 span 移到 `v-else` 之后、独立 `v-if`，链恢复连续；并加注释防回归 |
| `TemplatePreview` | 同类型分发 | 干净，单链以 `v-else` 兜底，无中断 |
| `ResumeVisual` | 图表类型分发（radar→…→statcard） | 干净，同级连续；内部 `v-if` 均嵌套在各分支内、作用域正确；无 `v-else` 无错配 |
| `InterviewView` | 面试阶段分发（intro→running→loading→report） | 干净，顶层连续；各 `v-else` 均嵌套在分支内、与相邻 `v-if` 正确配对 |
| `EditorView`/`ProfileView` 等 | 布尔开关（X / 非X） | 非分发链，逐一核对配对正确 |

**全局佐证**：`vite build` 无任何「`v-else`/`v-else-if` 缺少相邻 `v-if`」告警 → 不存在被普通元素截断的链。

**沉淀的规范**：类型/状态分发链里，**不要把独立用途的 `v-if` 元素插在最后一个 `v-else-if` 与兜底 `v-else` 之间**；这类浮层（字数、徽标等）应放在 `v-else` 之后并自带独立 `v-if`。该约定已以注释写入 `DragResumeCanvas`。

---

## 如何验证

```bash
# 后端测试
cd backend && mvn test            # 期望 Tests run: 18, Failures: 0

# 后端启动
java -jar backend/target/resume-lcode-backend-1.0.0.jar   # :8080 /api

# 前端（开发）
cd frontend && npx vite           # http://localhost:5173

# 前端构建（看拆包结果）
cd frontend && npx vite build     # 观察 dist/assets：echarts/element-plus 独立 chunk
```

安全回归手测建议：未登录直接 `GET /api/resumes` 应返回 401；登录后 A 用户传 `?userId=B` 不再能读到 B 的简历。

---

## 剩余风险与后续建议（按优先级）

1. **【严重·尽快】** 社区审核/删除接口加管理员鉴权（见 ②待办）。
2. **【高】** 社区/模板控制器统一改 `@CurrentUserId`，收口最后一批客户端 `userId`。
3. **【中】** Session Cookie 安全属性 + 防会话固定；去掉「null→演示用户 1」兜底。
4. **【中】** Element Plus 按需引入，进一步压首包。
5. **【低】** 给关键接口补一层 MockMvc 集成测试（覆盖拦截器+控制器真实链路）；清理后端少量未用 import/字段。

---

## 本次变更文件

**后端**
- 新增：`config/CurrentUserId.java`、`config/CurrentUserIdArgumentResolver.java`
- 改：`config/WebMvcConfig.java`、`controller/{Resume,Interview,Member}Controller.java`、`service/impl/FileUploadServiceImpl.java`
- 新增测试：`src/test/java/com/resume/**`（4 类 + `support/TestRepos`）

**前端**
- 改：`src/router/index.js`、`vite.config.js`
